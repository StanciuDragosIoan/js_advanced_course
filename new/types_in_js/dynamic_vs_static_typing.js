/*
    flow, RE, TS, elm 


    flow (uses bable to convert code to JS)

    TS uses TS engine

    RE (reason ML) uses a RE engine (its own language/engine)

    elm (same as RE has its own language/engine)

    TS is the most important
*/