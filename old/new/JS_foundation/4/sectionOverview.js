/*
    will go through how JS works

    JS engine, JS runtime, Interpreter/Compiler/JIT Compiler,
    Writing optimized code, 
    callStack, memory heap,
    stack overflow and memory leaks,
    garbage collection,
    node.js
    single threaded model of node
*/
