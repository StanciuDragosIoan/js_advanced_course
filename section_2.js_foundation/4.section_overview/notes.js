/*
    will build the foundation for JS and any language

    will cover: js engine, js runtime, interpreter/compiler/jit ocmpiler,
    call stack, memory heaps, stackoverflow and memory leaks, node.js, single threaded model..
*/